package project;

public interface Number {
    double sum(Number number);
    Number add(Number number);
    double sub(Number number);
    Number subtract(Number number);
    double mult(Number number);
    Number multiply(Number number);
    double divide(Number number);
    Number division(Number number);
    double getValue();
}
